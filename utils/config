require('dotenv').config()

const PORT = process.env.PORT
const urlBlog = process.env.NODE_ENV === 'test'
    ? process.env.testUrlBlog
    : process.env.urlBlog

module.exports = {
    PORT,
    urlBlog
}